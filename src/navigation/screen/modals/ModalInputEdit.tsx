import {ModalEdit} from 'components/base';
import React from 'react';

const ModalInputEdit = () => {
  return <ModalEdit />;
};

export default Object.assign(ModalInputEdit, {
  show: () => null,
});
